/* Author: Drew Chase */
$("tbody").html('<div class="loading"></div>'),$(document).on("computersLoaded",(o,t)=>{for(const o of t){let t=Computer.fromObject(o).toDOM();$("tbody").append(t)}$("tbody .loading").remove()});const computers=new Computers;